workflows:
  - name: "login"
    flow: "login"
    method_auth: "fido-pin"
    expected_tokens: [ "access-token" ]

scenarios:
  - name: "login-after-upgrade"
    description: "DEMO-1001: existing users can still log in after the upgrade"
    supported_namespaces: [ "dev-main", "dev-pkv" ]
    labels: [ "DEMO-1001", "regression", "00000000-0000-0000-0000-d0c0ffee001e" ]
    sequence: [ "login" ]
